queue<string> queue
    que.push()
    que.size(): total elements in the queue
    !que.empty(): used to check in the queue is empty
    que.pop()
    que.front(): agadi ko item variable ma halna lai



unordered_set<string> st
    st.contains()
    st.insert()
    st.erase()


unordered_map<int, int> myMap;
    myMap.insert({1,0})
    myMap[1] = 0


stack<char> st;
    st.top()
    st.push()
    st.empty()
    st.pop()
    st.size()


vector vaneko dynamic array
vector<int> freq(26,0);  //creates a vector names freq that has 26 elements, each initiated to 0
    freq.size();
    freq.push_back()